import PixelGridCanvas from "@/components/PixelGridCanvas";

export default function Home() {
  return (
    <main style={{ padding: 32, display: "flex", gap: 20 }}>
      <PixelGridCanvas />
    </main>
  );
}
